#include <stdio.h>

int main()
{
  int a,b,i;
  printf("enter your data:");
  scanf("%d %d",&a,&b);
  for(i=1;i<=a;i++)
  {
      if(i==b)
      {
          b++;
          break;
      }
  }
  printf("%d\n",b);

    return 0;
}
